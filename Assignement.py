import random
def newrange(n):
    if n in range(random):
        print( "The number is in the range")
    else :
        print("The number is outside the given range.")
a = int(input("Enter a number:"))
newrange(a)
